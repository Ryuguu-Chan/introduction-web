const chat = {
    couleur: function(nomCouleur) {
        if (typeof nomCouleur == 'string') {
            alert("Il faut écrire la couleure de la façon suivante\n\nchat.couleur(\"" + nomCouleur + "\")");
        }
    }
};